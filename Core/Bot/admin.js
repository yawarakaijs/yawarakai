// Local Files

let Store = require('../storage')
let config = require('../../config.json')

let Control = {
    start(context) {
        let id = context.ctx.message.from.id
        let ctx = context.ctx
    }
}

module.exports = {
    Control
}